<% content_for(:title, @question.title) %>
<div>
  <div>
    <div>
      <h1><%= t('.title') %></h1>
      <%= render 'form', { question: @question, tag_list: @tag_list } %>
    </div>
  </div>
</div>