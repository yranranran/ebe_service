<div>
  <div>
    <%= form_with model: answer, url: [question, answer], id: 'new_answer', local: false do |f| %>
      <%= f.label :body %>
      <%= f.text_area :body, id: 'js-new-answer-body', row: 4, placeholder: Answer.human_attribute_name(:body) %>
      <%= f.submit t('defaults.post') %>
    <% end %>
  </div>
</div>